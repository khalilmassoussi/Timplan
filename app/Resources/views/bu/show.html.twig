{% extends 'base.html.twig' %}

{% block body %}
    <h1>Bu</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bu.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ bu.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('bu_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('bu_edit', { 'id': bu.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
