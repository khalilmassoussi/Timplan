{% extends 'base.html.twig' %}

{% block body %}
    <h1>Affaire</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ affaire.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ affaire.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('affaire_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('affaire_edit', { 'id': affaire.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
